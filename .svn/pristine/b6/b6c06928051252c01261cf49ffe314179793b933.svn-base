import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { IonicModule } from '@ionic/angular';

import { TourItenaryComponent } from './tour-itenary.component';

describe('TourItenaryComponent', () => {
  let component: TourItenaryComponent;
  let fixture: ComponentFixture<TourItenaryComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TourItenaryComponent ],
      imports: [IonicModule.forRoot()]
    }).compileComponents();

    fixture = TestBed.createComponent(TourItenaryComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
